% ----- Preambule (pastikan sudah ada graphicx dan geometry) -----
\documentclass[12pt,a4paper]{article}
\usepackage{graphicx}
\usepackage{geometry}
\usepackage{setspace}
\usepackage{ragged2e}
\usepackage{array}
% ----- IEEE Bibliography -----
\usepackage[backend=biber,style=ieee]{biblatex}
\addbibresource{references.bib} % file .bib yang akan dibuat

\geometry{left=3cm,right=3cm,top=2.5cm,bottom=3cm}
\setstretch{1.25}

% optional: search images in images/ folder
\graphicspath{{./}{images/}}

% ----- Header: logo, institute, metadata -----
\begin{document}

% Centered logo + institute title
\begin{center}
  % adjust width as needed, file must exist in project folder
  \includegraphics[width=4.5cm]{logo-itera.png} \\[6pt]
  {\Large\bfseries Program Studi Teknik Informatika}\\[2pt]
  {\large Institut Teknologi Sumatera}
\end{center}

\vspace{8pt}

% Thick horizontal rule across page (like sample)
\noindent\hrule height 2pt
\vspace{6pt}

% Two-line metadata, aligned left/right (use p-columns for wrapping)
\noindent
\begin{tabular}{@{} p{0.62\textwidth} p{0.36\textwidth} @{}}
  \textbf{Nama:} Bintang Fikri Fauzan, M. Fakhri Nur, Rafki Haykhal Alif & 
  \textbf{Tugas:} \textbf{Final Project} \\
  \textbf{Mata Kuliah:} Sistem/Teknologi Multimedia (IF4021) &
  \textbf{Tanggal:} 11 Desember 2025
\end{tabular}

\vspace{6pt}

% Thin horizontal rule under metadata (like sample)
\noindent\hrule height 0.8pt
\vspace{12pt}
% --------------------------------------------------
% DESKRIPSI PROJECT
% --------------------------------------------------
\section{Deskripsi Project}

AirBeats -- Touchless Piano Tiles dikembangkan sebagai permainan multimedia interaktif yang memanfaatkan deteksi \textit{gesture} tangan secara \textit{real-time} untuk menghasilkan pengalaman bermain tanpa sentuhan. Tujuan utama proyek ini adalah menyediakan mekanisme interaksi yang lebih natural dan higienis melalui pemetaan gerakan \textit{tap} pada ruang udara ke dalam aksi menekan \textit{tile} musik. Konsep \textit{gesture detection} diterapkan dengan menangkap pergerakan jari melalui kamera dan menerjemahkannya menjadi input yang memicu suara piano serta evaluasi skor permainan. \textit{Gameplay} berfokus pada ketepatan waktu pemain dalam menekan \textit{tile} yang jatuh, sehingga unsur audio–visual bekerja secara sinkron untuk mendukung respons imersif. Dari perspektif multimedia, proyek ini relevan karena menggabungkan pemrosesan video, audio dinamis, serta umpan balik visual secara terintegrasi dalam satu sistem interaktif. Teknologi \textit{touchless interaction} memberikan manfaat berupa pengalaman bermain yang lebih intuitif, responsif, dan sesuai dengan tren \textit{contact-free interface} yang semakin banyak diadopsi dalam aplikasi modern.

% --------------------------------------------------
\section{Teknologi}

\subsection{Python}

Python adalah bahasa pemrograman tingkat tinggi yang dirancang untuk komputasi umum, sains data, dan aplikasi multimedia berkat sintaksnya yang sederhana serta ekosistem \textit{library} yang luas. Dalam sistem multimedia, Python berperan sebagai pengelola alur logika, integrasi modul visual dan audio, serta platform utama pemrosesan \textit{real-time}. Pada AirBeats, Python digunakan untuk mengatur seluruh struktur permainan, termasuk deteksi \textit{gesture}, manajemen \textit{tile}, audio, \textit{state machine}, dan \textit{rendering} visual \cite{python}.

\subsection{MediaPipe Hands}

MediaPipe Hands merupakan model deteksi tangan \textit{real-time} yang memetakan 21 titik \textit{hand landmark} menggunakan pendekatan \textit{machine learning} dan \textit{graph-based processing pipeline}. Teknologi ini umum digunakan untuk \textit{gesture recognition}, interaksi tanpa sentuhan, AR/VR, dan aplikasi visi komputer. Dalam AirBeats, MediaPipe menangkap posisi jari pemain dan memberikan koordinat akurat untuk menentukan aksi \textit{tap} berdasarkan perubahan posisi vertikal jari \cite{mediapipe}.

\subsection{OpenCV (cv2)}

OpenCV adalah pustaka visi komputer \textit{open-source} yang menyediakan fungsi untuk membaca video, memproses citra, dan melakukan transformasi visual secara efisien. Teknologi ini menjadi standar dalam aplikasi multimedia seperti pendeteksian objek, pemrosesan frame kamera, dan integrasi sistem \textit{real-time}. Pada AirBeats, OpenCV bertugas menangkap input kamera, mengirimkan frame ke MediaPipe, dan menyediakan \textit{NumPy array} yang kemudian ditampilkan kembali melalui Pygame sebagai latar visual permainan \cite{opencv}.

\subsection{NumPy}

NumPy adalah pustaka komputasi numerik yang menyediakan struktur \textit{array} multidimensi serta operasi matematika berkecepatan tinggi. NumPy menjadi fondasi bagi banyak sistem pemrosesan multimedia dan visi komputer. Dalam AirBeats, NumPy digunakan untuk menangani data frame dari OpenCV, perhitungan pergerakan jari antar frame, serta operasi matematis lain yang diperlukan oleh modul deteksi \textit{gesture} \cite{numpy}.

\subsection{Pygame}

Pygame adalah pustaka multimedia berbasis Python untuk menangani grafis 2D, audio, \textit{event handling}, dan \textit{game loop} secara \textit{real-time}. Dalam konteks multimedia, Pygame digunakan untuk mengembangkan aplikasi interaktif yang memerlukan sinkronisasi visual dan audio. AirBeats memanfaatkan Pygame untuk menampilkan \textit{tile} jatuh, HUD skor, efek visual, serta memainkan suara piano setiap kali \textit{gesture tap} terdeteksi \cite{pygame}.

% --------------------------------------------------
\section{Cara Kerja Program}
% --------------------------------------------------

\begin{itemize}

\item \textbf{Kamera Membaca Frame} \\
Proses dimulai ketika sistem menangkap frame secara kontinu melalui \textit{webcam} menggunakan OpenCV. Setiap frame dibaca dalam format BGR kemudian dikonversi menjadi RGB agar kompatibel dengan MediaPipe. Frame ini juga dapat diperbesar atau disesuaikan ukurannya sebelum diproses lebih lanjut. Setelah itu, frame disimpan sebagai \textit{surface} yang dapat ditampilkan kembali pada layar permainan melalui Pygame. Tahap ini menjadi fondasi bagi seluruh proses interaksi berbasis gesture.

\item \textbf{MediaPipe Mendeteksi Tangan} \\
Setelah frame diperoleh, MediaPipe Hands melakukan analisis untuk mendeteksi posisi tangan dan menghasilkan 21 titik \textit{landmark} untuk tiap tangan. Titik-titik ini mewakili sendi dan ujung jari, sehingga sistem dapat mengetahui posisi jari pemain secara presisi. Koordinat yang dihasilkan kemudian dinormalisasi ke dalam dimensi frame sehingga dapat dipakai dalam analisis temporal. Jika tangan tidak terdeteksi, sistem tetap melanjutkan pembacaan frame berikutnya tanpa memicu input. Tahap ini memastikan permainan selalu mendapatkan data gesture yang stabil dan akurat.

\item \textbf{Deteksi Gesture Tap} \\
Deteksi gesture dilakukan dengan memantau perubahan posisi vertikal pada jari tertentu seperti jari telunjuk, tengah, manis, dan kelingking. Jika penurunan posisi jari melewati ambang batas tertentu, gerakan tersebut ditafsirkan sebagai \textit{tap}. Pendekatan ini efektif karena gerakan tap umumnya diawali dengan pergerakan cepat ke bawah yang mudah diukur secara numerik. Proses smoothing diterapkan untuk mengurangi noise agar deteksi lebih konsisten pada berbagai kondisi. Hasil deteksi gesture kemudian diteruskan ke modul gameplay sebagai input lane.

\item \textbf{Game Manager Menerima Input Lane} \\
Ketika gesture tap terdeteksi, sistem mengirimkan input berupa nomor lane ke \texttt{GameManager}. Modul ini memverifikasi apakah permainan sedang berada pada state yang menerima input, yaitu mode gameplay aktif. Selanjutnya, \texttt{GameManager} meneruskan input tersebut ke \texttt{TileManager} untuk memeriksa apakah ada tile yang berada di \textit{hit zone}. Modul ini juga menangani pembaruan statistik seperti jumlah tap, waktu permainan, dan kondisi transisi state. Dengan demikian, \texttt{GameManager} berfungsi sebagai penghubung antara gesture pemain dan logika inti permainan.

\item \textbf{Tile System Menjatuhkan Tile dan Mengecek Hit/Miss} \\
Tile dijatuhkan berdasarkan waktu kemunculan yang ditentukan oleh lagu melalui \texttt{SongManager}. Setiap tile bergerak ke bawah dengan kecepatan sesuai tingkat kesulitan hingga mencapai area \textit{hit zone}. Saat pemain melakukan tap, \texttt{TileManager} memeriksa apakah tile berada dalam zona tersebut dan menentukan kualitas hit seperti \textit{PERFECT}, \textit{GOOD}, atau \textit{BAD}. Jika tile lewat tanpa terkena tap, tile dianggap miss dan dicatat sebagai kesalahan pemain. Sistem tile ini memastikan sinkronisasi antara musik, pergerakan tile, dan gesture pemain.

\item \textbf{Audio Manager Memainkan Note} \\
Ketika tile berhasil di-hit, \texttt{AudioManager} memainkan nada piano yang sesuai dengan tile tersebut. Pemutaran audio dilakukan menggunakan \texttt{pygame.mixer} yang mampu menghasilkan suara dengan latensi rendah. Setiap nada disimpan sebagai file WAV sehingga respons audio menjadi cepat dan jernih saat dipicu oleh gesture pemain. Proses ini berjalan paralel dengan logika gameplay sehingga tidak mengganggu \textit{frame rendering}. Dengan demikian, pemain mendapatkan pengalaman audio–visual yang sinkron dan responsif.

\item \textbf{Skor, Combo, dan State Game Diperbarui} \\
Setelah sistem menentukan nilai hit, \texttt{ScoreManager} menambahkan poin sesuai kualitas pukulan pemain. Pada saat yang sama, \texttt{ComboCounter} meningkatkan atau mereset combo tergantung kondisi hit atau miss. Informasi ini kemudian ditampilkan pada HUD permainan agar pemain dapat memantau performa mereka secara \textit{real-time}. Sementara itu, \texttt{GameManager} mengevaluasi apakah kondisi tertentu telah tercapai, seperti \textit{game over} akibat terlalu banyak miss. Tahap ini memastikan permainan berjalan dinamis dan evaluatif hingga lagu berakhir.

\end{itemize}

% --------------------------------------------------
% Daftar Pustaka (IEEE via biblatex)
% --------------------------------------------------
\newpage
\printbibliography[title={Daftar Pustaka}]

\end{document}

